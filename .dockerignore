# Git
.git
.gitignore
.github

# Terraform
.terraform
*.tfstate
*.tfstate.backup

# IDE
.idea
.vscode
*.swp
*.iml

# Logs
*.log
logs/

# Local binaries (except the ones we explicitly build for release)
# We want to allow bin/ so Dockerfile.fast works
# But we might want to ignore other build artifacts if they exist
# For now, let's keep bin/ allowed because we use it.

# Go
vendor/
go.work

# OS
.DS_Store
Thumbs.db
