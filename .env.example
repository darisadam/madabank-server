# Server Configuration
ENV=development
# Port for the application (internal docker port)
PORT=8080

# Database Configuration
# Format: postgres://user:password@host:port/dbname?sslmode=disable
# Use 'postgres' as host when running inside docker-compose network
DATABASE_URL=postgres://madabank_user:secure_password_placeholder@postgres:5432/madabank?sslmode=disable

# Redis Configuration
# Use 'redis' as host when running inside docker-compose network
# For password-protected Redis (production): redis://:password@redis:6379
REDIS_URL=redis:6379
# Optional: Redis password (leave empty for local dev)
REDIS_PASSWORD=

# Security & Auth
# CRITICAL: Use a strong, random 64-character string for JWT_SECRET
JWT_SECRET=change_me_to_a_super_strong_random_secret_string
JWT_EXPIRY_HOURS=24

# Data Encryption
# CRITICAL: Must be exactly 32 characters long
ENCRYPTION_KEY=change_me_32_chars_exact_string!!

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_DURATION=1m